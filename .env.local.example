  # This project loads ANTHROPIC_API_KEY from a per-project secrets file outside of the Claude Devcontainer:
  #   ~\contract-triage.env
  # The loaders (next.config.mjs / scripts/test-triage.ts) use dotenv.parse()
  # and copy ONLY ANTHROPIC_API_KEY into process.env.
  # For Vercel: set ANTHROPIC_API_KEY in the project dashboard.

  ANTHROPIC_API_KEY=sk-ant-...
