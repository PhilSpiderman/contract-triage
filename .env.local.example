# This file documents the env vars the app needs.
# The real key is loaded from C:\Users\Chris\secretsecrets\.env
# via next.config.mjs — you don't need a local .env.local for dev.
#
# For Vercel deployment, set ANTHROPIC_API_KEY in the project dashboard.

ANTHROPIC_API_KEY=sk-ant-...
